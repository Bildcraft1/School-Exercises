#include <iostream>
#include <cstdlib>
#include <string>

using namespace std;

void leggi(string V[], int DIM)
{
    for (int i = 0; i < DIM; i++)
    {
        cout << V[i] << endl;
    }
}

void leggi(int V[], int DIM)
{
    for (int i = 0; i < DIM; i++)
    {
        cout << V[i] << endl;
    }
}

void leggi(float V[], int DIM)
{
    for (int i = 0; i < DIM; i++)
    {
        cout << V[i] << endl;
    }
}